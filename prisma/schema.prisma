generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"  // Change from "sqlite"
  url      = env("DATABASE_URL")
}

model User {  // Example for NextAuth
  id    String @id @default(cuid())
  email String @unique
  // Add your fields
}